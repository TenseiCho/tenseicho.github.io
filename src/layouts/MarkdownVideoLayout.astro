---
import BaseLayout from './Layout.astro';
const { frontmatter } = Astro.props;
---
<BaseLayout pageTitle={frontmatter.title}>
    <h2>{frontmatter.title}</h2>
    <div class="project-image-container">
        <img class="project-image" src={frontmatter.image.url} alt={frontmatter.image.alt} />
    </div>
    <p>{frontmatter.description}</p>
    <div class="social-links">
        <ul>
            <li><strong>YouTube:</strong> <a href={frontmatter.youtubeLink} target="_blank">Watch on YouTube</a></li>
            {frontmatter.socialmediaLink && (
                <li><strong>X:</strong> <a href={frontmatter.socialmediaLink} target="_blank" class="social-link">Follow on X</a></li>
            )}
        </ul>
    </div>
    <slot />
</BaseLayout>

<style>
    .project-image-container {
        width: 500px;
        max-width: 100%;
        margin: 0 auto;
    }
    .project-image {
        width: 100%;
        height: auto;
        border-radius: 8px;
    }
    .social-links {
        margin: 1rem 0;
    }
    .social-links ul {
        list-style: none;
        padding: 0;
    }
    .social-links li {
        margin: 0.5rem 0;
    }
    .social-links a {
        color: var(--text-link);
        text-decoration: none;
        cursor: pointer;
    }
    .social-links a:hover {
        text-decoration: underline;
    }
    .social-link {
        display: inline-block;
        pointer-events: auto;
    }
</style> 